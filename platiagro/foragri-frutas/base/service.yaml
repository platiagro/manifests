apiVersion: v1
kind: Service
metadata:
  name: foragri-frutas
  annotations:
    getambassador.io/config: |-
      ---
      apiVersion: ambassador/v0
      kind:  Mapping
      name: foragri-frutas-mapping
      prefix: /
      timeout_ms: 300000
      service: $(service).$(ui-namespace)
      use_websocket: true
  labels:
    app: foragri-frutas
spec:
  ports:
  - port: 80
  selector:
    app: foragri-frutas