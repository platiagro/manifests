apiVersion: v1
kind: Service
metadata:
  name: model-drift-explorer
  annotations:
    getambassador.io/config: |-
      ---
      apiVersion: ambassador/v0
      kind:  Mapping
      name: model-drift-explorer-mapping
      prefix: /explorer
      timeout_ms: 300000
      service: $(service).$(ui-namespace)
      use_websocket: true
  labels:
    app: model-drift-explorer
spec:
  ports:
  - port: 3838
  selector:
    app: model-drift-explorer